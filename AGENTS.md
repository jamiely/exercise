# Local Agent Rules

- Always ensure tests pass.
- Always run lint.
- Add Playwright tests for any new features.
- Add missing Playwright tests for key features and critical user flows.
- If unsure whether a feature is key, ask for clarification before implementing.
- Track and report unit test coverage with `npm run test:coverage`.
- Ensure coverage in all categories (statements, branches, functions, lines) does not drop below 90%.
- Run Prettier for formatting.
- Commit after every feature is complete.
- Ask clarifying questions to improve feature quality before implementation.
